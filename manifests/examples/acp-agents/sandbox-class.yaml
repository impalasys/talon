apiVersion: talon.impalasys.com/v1
kind: SandboxClass
metadata:
  name: docker-codex
  namespace: Example
  labels:
    example: acp-agents
spec:
  provider: docker
  providerConfig:
    image: talon-codex-acp:local
  credentials: {}
